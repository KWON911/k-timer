# 수업용 집중 타이머 — 구현 계획

> 상태: **구현 완료** (2026-07-30) · 산출물 `focus-timer.html`
> 계획 원본은 `~/.claude/plans/vivid-foraging-scroll.md` 에도 보관돼 있습니다.

## Context

수업 중 활동 시간을 관리할 타이머가 필요하다. 교실 프로젝터/TV에 띄워 학생 전체가 남은 시간을 보고,
교사는 같은 화면에서 프리셋·스텝을 조작한다. 상용 웹 타이머는 인터넷 의존·광고·학교망 차단 문제가 있어
**의존성 0의 단일 HTML 파일**로 만든다. USB로 복사해 어느 교실 PC에서든 더블클릭으로 실행되는 것이 목표다.

**결정된 요구사항**
- 형태: 단일 HTML 파일 (CSS/JS 인라인, 외부 요청 0, 오프라인 동작)
- 화면: 큰 화면 중심 + 여닫는 설정 패널
- 기능: 프리셋 버튼 / 정각 종료 / 뽀모도로 반복 / 소리·시각 알림 / 활동 스텝 목록 / 수업 템플릿 여러 개 저장
- 표시: **링 · 원판(타임타이머식) · 숫자만** 중 선택, 단계별 색상 + 기본 색상 직접 지정, 다크·라이트 테마
  (최초 요구사항은 "원형 게이지 + 숫자만 모드 토글" 이었고, 이후 표시 방식 3종 선택으로 확장됐다)

## 산출물

`focus-timer.html` — 단일 파일, 1565줄 / 75.4KB.

`index.html` — GitHub Pages 용 리다이렉트 한 장 (https://kwon911.github.io/k-timer/).
Pages 는 폴더 주소에서 `index.html` 을 찾는데, 본체 이름을 `index.html` 로 바꾸면
USB 배포에서 무슨 파일인지 알 수 없고 문서·커밋에 적힌 이름도 전부 어긋난다.
그래서 이름은 지키고 넘겨보내는 파일만 따로 뒀다. `meta refresh` 가 막히는 환경을 위해
손수 누를 링크도 함께 둔다. 상대 경로라 외부 요청은 여전히 0건이다.

## 설계

### 0. 정각 종료 모드 (벽시계 기준)

"25분"이 아니라 **"10시 50분까지"** 를 입력하는 방식. 수업은 길이가 아니라 끝나는 시각이 정해져 있고,
늦게 시작하면 프리셋으로는 매번 남은 시간을 머리로 계산해야 한다.

엔진이 이미 절대 마감시각으로 돌아가므로 추가 비용이 거의 없다 — 목표 시각을 그대로 `endsAt`에 넣는다.

```js
state.absoluteEnd     // 목표 시각 (없으면 일반 카운트다운)
start():  endsAt = state.absoluteEnd || (Date.now() + remainingMs)
armStep(): absoluteEnd 가 있으면 remainingMs 를 목표 시각까지 다시 계산
```
- **일시정지해도 종료 시각은 밀리지 않는다.** 벽시계는 멈추지 않으므로, 재개하면 목표 시각으로 되돌아간다.
  정지 중 표시값만 멈춰 있고, 재개 시 정지 동안 흐른 만큼 줄어 있다. 일반 카운트다운과 의미가 다른 지점이다.
- `parseClock("10:50")` → 오늘의 그 시각. **이미 지났으면 `null`** — 자정을 넘겨 24시간 타이머가
  되는 사고를 막는다. 12시간 상한도 둔다.
- 목표 시각이 지난 뒤 "다시 시작"을 누르면 조용히 아무 일도 안 하는 대신 안내 문구를 띄운다.
- `loadQueue()`가 `absoluteEnd`를 해제하므로, 다른 모드로 넘어갈 때 정각 상태가 남지 않는다.

### 0-1. 단계 목록 자동 압축 (`fitStepsTo`)

수업이 늦게 시작됐을 때, 45분 계획을 남은 36분에 **비율대로** 맞춰 실행한다.

- **원본 `stepList` 는 건드리지 않는다.** 압축은 실행 큐에만 적용된다 — 교사의 수업 계획과
  저장된 템플릿이 파괴되면 안 된다.
- **누적값 반올림**으로 배분한다. 각 단계를 따로 `round` 하면 합계가 목표에서 어긋나므로,
  누적 합을 반올림하고 차분을 단계 길이로 쓴다. 마지막 단계는 남은 전부를 받아 **합계가 정확히 일치**한다.
- 어떤 단계가 5초 미만이 되면 거부(`tooshort`). 지난 시각·빈 목록도 거부.
- 계획보다 시간이 남으면 늘리는 방향으로도 동작한다.
- 이 모드는 `absoluteEnd`를 쓰지 않는다. 단계 길이의 합이 정확하므로 정지 없이 진행하면 목표 시각에 끝나고,
  **일시정지하면 그만큼 뒤로 밀린다**(단일 정각 타이머와 다른 지점). 중간에 다시 압축하려면 버튼을 다시 누른다.

### 0-2. 실행 중 초기화 보호 (`guardedReset`)

`R` 키와 초기화 버튼은 실행 중이고 남은 시간이 3초를 넘을 때 `confirm` 으로 한 번 확인한다.
교실에서 잘못 눌리면 진행 중인 활동이 그대로 날아가기 때문이다. 남은 시간이 거의 없으면 묻지 않는다.
마감시각 기준이라 `confirm` 이 스레드를 붙잡고 있는 동안에도 시간은 정확히 흐른다.

### 1. 타이머 엔진 — 마감시각(deadline) 기반

`setInterval`로 1초씩 빼면 드리프트가 쌓이고, 브라우저가 백그라운드 탭의 타이머를 1초 이상으로
throttle 해서 시간이 밀린다. 대신 **절대 마감시각을 저장하고 매 틱마다 남은 시간을 계산**한다.

```js
// 시작:     endsAt = Date.now() + remainingMs
// 틱(200ms): remainingMs = Math.max(0, endsAt - Date.now())
// 일시정지:  remainingMs 확정 저장, endsAt = null
```
- 틱 주기는 200ms. 실제 렌더는 표시값이 바뀔 때만.
- 백그라운드에 있다 돌아와도 `Date.now()` 차이로 즉시 정확한 값 복원. `visibilitychange`에서 강제 재렌더.
- **단계 전환 시 이전 단계의 계획된 마감시각을 기준점으로 삼는다** (`endsAt = plannedEnd + nextMs`).
  단계마다 `Date.now()`로 다시 시작하면 틱 주기만큼(최대 200ms) 오차가 누적된다.

### 2. 상태 모델 — 뽀모도로와 스텝을 하나의 큐로 통합

핵심 단순화: **뽀모도로는 자동 생성된 스텝 큐**로 취급한다. 엔진은 "스텝 큐를 순서대로 실행"하는
로직 하나만 갖고, 단순 프리셋은 길이 1의 큐가 된다. 세 모드에 각각 다른 진행 코드를 쓰지 않는다.

```js
const state = {
  queue: [{ name, ms, kind }],   // kind: 'focus' | 'break' | 'longBreak'
  index: 0,
  status: 'idle' | 'running' | 'paused' | 'finished',
  remainingMs, endsAt, totalMs,  // totalMs = 현재 단계 전체 길이(게이지 기준)
  settings: { theme, display, baseColor, sound, volume, autoAdvance, panelOpen,
              preWarn, overrun, showClock, openSecs },   // openSecs = 펼쳐 둔 설정 구획 id 배열
  pomodoro: { focusMin, breakMin, longBreakMin, totalRounds, roundsPerLongBreak },
  stepList: [{ name, minutes }]  // 사용자가 편집하는 원본
};
```
- 단계 종료 → `autoAdvance` 켜짐이면 다음 단계 자동 시작, 꺼짐이면 다음 단계를 장전한 채 대기.
- 큐 마지막 종료 → `status = 'finished'`, 종료 연출.

### 3. 화면 구성

```
┌──────────────────────────────────────────┬──────────────┐
│  모둠 토의                    3 / 5      │  설정 패널    │
│                                          │  (슬라이드)   │
│         ╭───────────╮                    │ 빠른 시작    │
│         │   14:23   │  ← 게이지(3종)     │ 활동 단계    │
│         ╰───────────╯                    │ 뽀모도로     │
│                                          │ 표시·소리    │
│      다음: 발표 · 10분                   │ 단축키       │
│  [시작] [초기화] [다음 ▶] [⛶ 전체화면]   │              │
└──────────────────────────────────────────┴──────────────┘
```
- CSS Grid 2열. 패널은 `margin-right: calc(-1 * var(--panelW) - 8px)` + `transition`으로 여닫음(닫히면 그리드 트랙이 0으로 줄어 스테이지가 전체 폭 사용).
- 조작 버튼은 실행 중 유휴 3초 후 흐려지고(`body.dim-controls`), 마우스 이동 시 복귀.

**반응형 — 폭과 높이를 따로 본다**

처음에는 `860px` 하나로 갈랐다. 실제로 재 보니 두 가지가 깨져 있었다.

- **낮은 창에서 아래 버튼이 잘렸다.** 게이지가 `min(68vmin, 660px)` 이라 세로 여유를 보지 않았다.
  노트북 창을 반만 쓰거나 휴대전화를 가로로 들면 폭은 넉넉한데 높이가 부족해지고, 그때 밀려나는 건 조작 버튼이다.
- **860px 미만의 오버레이가 타이머를 반쯤 덮었다.** 패널이 스테이지 위로 떠서 게이지와 시작 버튼을 가렸다.

그래서 게이지를 **두 축의 최솟값**으로 정한다. `--pw` 는 패널이 차지한 폭, `--chrome` 은 제목·안내·버튼이 쓰는 높이다.

```css
--gauge:min(calc(100vw - var(--pw) - 6vmin), calc(100vh - var(--chrome)), 660px);
```

폭 단계 — 패널 폭(`--panelW`)을 줄여 스테이지에 자리를 넘긴다. `.panel` 의 `width`,
닫힘 상태의 `margin-right`, 스테이지의 `--pw` 가 전부 이 변수를 읽으므로 한 곳만 고치면 셋이 같이 따라온다.

| 폭 | `--panelW` | 패널 형태 |
|---|---|---|
| 1200px 이상 | 352px | 그리드 열 |
| 900–1199px | 312px | 그리드 열 |
| 681–899px | 280px | 그리드 열 (패널 속 칸도 좁게 다시 잡음) |
| 680px 미만 | `100vw` | **전체 화면 시트** |

`680px` 에서 끊은 근거: 그 아래로 패널을 나란히 두면 스테이지에 남는 폭이 360px 밑으로 떨어진다.
그러면 조작 버튼 4개가 두 줄로 접히고, 늘어난 높이만큼 게이지가 밀려 아래가 잘린다.
**"둘 다 조금씩" 대신 "패널을 볼 때는 패널만"** 으로 끊는 편이 낫다 — 시트를 닫으면 타이머가 화면을 온전히 쓴다.

높이 단계 — 세로가 부족할 때 여백부터 회수한다.

| 높이 | 조정 |
|---|---|
| 560px 미만 | `--chrome` 을 `clamp(112px,26vh,180px)` 로, 여백·간격 축소, **조작 버튼을 한 줄에 붙도록 작게** |
| 420px 미만 | `.stage-head` · `#note` 의 최소 높이 회수 (`--chrome` 은 더 줄이지 않는다) |

- 420px 미만에서 `--chrome` 을 더 낮추지 않는 이유: 제목·버튼의 고정 비용이 대략 거기서 바닥이다.
  더 낮추면 게이지가 그만큼 커지면서 버튼이 화면 밖으로 밀린다 (측정으로 확인).
- 560px 미만에서 버튼을 작게 만드는 이유는 미관이 아니다. 두 줄로 접히면 그 높이를 `--chrome` 이
  미리 알 수 없어서 잘림으로 이어진다. 줄 수를 1로 고정하는 것이 목적이다.
- `#panelBtn` 의 `z-index` 를 5 → 7 로 올렸다. 패널(6)보다 위여야 좁은 화면에서도 그 버튼으로 패널을 닫을 수 있다.

**설정 패널 — 접이식 구획**

기능이 늘면서 패널을 열면 다섯 구획이 한꺼번에 쏟아져 나와 수업 중에 원하는 항목을 찾기 어려웠다.
평소에는 **제목만** 보이고, 제목을 눌러야 그 안이 펼쳐지도록 바꿨다.

- 직접 만든 아코디언 대신 `<details>`/`<summary>` 를 썼다. 여닫는 동작·`Enter`/`Space` 조작·
  포커스 처리·스크린리더의 펼침 상태 안내가 전부 브라우저 기본으로 따라온다. JS 는 상태 저장만 한다.
- **동시에 여러 개를 펼 수 있게** 두었다. 하나만 열리는 아코디언은 "단계를 보면서 소리를 끄는" 식의
  흔한 조작에서 매번 되짚어야 해서 오히려 번거롭다.
- 열어 둔 구획은 `settings.openSecs` 에 id 배열로 저장한다. 저장값은 `SEC_IDS` 로 걸러 모르는 id 를 버리고,
  배열이 아니면 기본값(`["secQuick"]`)으로 떨어뜨린다 — openSecs 가 없던 예전 저장값도 그대로 동작한다.
- `toggle` 이벤트 처리기는 **값이 실제로 달라졌을 때만** 저장한다. `applySettings` 가 복원하며
  `el.open` 을 건드릴 때 되돌아 저장하는 것을 막고, 아무것도 바꾸지 않은 첫 실행에서
  localStorage 를 쓰지 않는 성질(OS 다크모드 자동 감지)을 유지하기 위해서다.
- 제목 글자는 `.78rem` 대문자 라벨에서 `1.06rem` 굵은 글씨로 키웠다 — 이제 이게 유일한 조작 대상이다.
- 패널 위쪽 `padding-top:54px` 는 `position:fixed` 인 ⚙설정 버튼이 첫 제목을 덮지 않게 비워 둔 자리다.

**큰 숫자 처리**
- `font-size: calc(var(--gauge) * .235)` — 게이지 크기에 비례해 원 안에 맞춘다(`--gauge` 산출은 위 반응형 절 참고).
- `font-variant-numeric: tabular-nums` — 숫자 폭이 흔들려 좌우로 튀는 현상 방지.
- `H:MM:SS`(6자 이상)일 때 `.long` 클래스로 크기를 줄여 원을 넘지 않게 한다.
- 시스템 폰트만 사용 — 웹폰트 요청 없음.

**원형 게이지 (SVG)**
- `viewBox="0 0 100 100"`, `r=45` → 원주 `2π·45 ≈ 282.743`.
- 배경 원 + 진행 원. 진행 원에 `stroke-dasharray: 282.743`, `stroke-dashoffset`을 남은 비율로 갱신.
- `transform: rotate(-90deg)`로 12시 방향에서 시작.

**표시 방식 — `ring` / `disk` / `number`**

`<html data-display="...">` 속성 하나로 CSS가 갈아끼운다. JS 분기 없이 처리하고, `M` 키가 순환한다.
독립 토글을 여러 개 두지 않고 **하나의 배타적 선택**으로 묶었다 — "숫자만 + 원판" 같은 무의미한 조합이 생기지 않는다.

- `ring`: 기존 얇은 링 + 큰 숫자 (`calc(--gauge * .235)`)
- `disk`: 교실용 타임타이머식 부채꼴 + 가운데 숫자
- `number`: 장식 전부 숨김, 숫자를 폭·높이 양쪽으로 조인다 (아래)

`number` 모드에는 원이 없어 **글자 크기가 곧 레이아웃**이다. 원래 `min(30vmin, 25rem)` 이었는데
`vmin` 은 패널이 차지한 폭도 창 높이도 보지 않아서, 좁은 창에서 숫자가 패널 밑으로 삐져나갔다.
`"05:00"` 의 실제 폭은 글자 크기의 약 2.5배(`H:MM:SS` 는 3.5배)로 측정됐고, 그 비율을 되짚어 폭에서 역산한다.

```css
font-size:min(calc((100vw - var(--pw) - 8vmin) * .38), calc(100vh - 96px), 25rem);
```

**원판(`disk`) 기하** — viewBox `0 0 100 100`, 중심 50,50.
- 부채꼴은 12시에서 시계방향으로 `remaining/total × 360°`. 남은 시간이 줄면 **12시로 수축**한다(타임타이머와 같은 방향).
- **360°는 호의 시작점=끝점이 되어 그려지지 않는다.** 이 경우만 중심선 없는 원 두 개로 나눠 그린다.
- **눈금(60칸, 5칸마다 굵게)을 부채꼴 반지름 밖에 둔다**(눈금 46→40, 부채꼴 38.5). 색 위에 겹치면
  진한 색에서 눈금이 사라지는데, 배경 위에만 그리면 어느 색에서도 항상 읽힌다.
- **한 바퀴 = 그 단계 전체 시간.** 그래서 눈금에 숫자를 붙이지 않는다 — 5분 단계에서 "10분" 눈금은 거짓이 된다.
  (60분 고정 방식은 5분 단계가 원판의 1/12만 차서 교실에서 잘 안 보인다.)
- 숫자는 가운데 구멍(r=22, 지름 44) 안에 들어가야 해 링 모드보다 작다: `.125 × gauge`
  (5자 폭 ≈ 3×글자크기 = 37.5 < 44). 시간 단위 표기는 `.09`.
- 부채꼴 경로는 **0.5° 단위로만** 다시 만든다. 200ms마다 경로 문자열을 새로 만들 필요가 없다.

**링 회전 처리** — 기존에는 `<svg>` 전체를 `rotate(-90deg)` 해 12시부터 시작했다. 원판은 자체 좌표계를 쓰므로
svg를 돌리면 어긋난다. **회전을 링 `<g>` 로만 옮겼다**(`transform="rotate(-90 50 50)"`).

**설정 이전** — 예전 버전의 `settings.minimal`(불리언)을 `settings.display`(문자열)로 옮긴다.
판단은 **저장된 객체 자체**를 봐야 한다. 병합 후의 `state.settings.display` 는 이미 기본값 `"ring"` 이라
항상 유효해 보여서, 저장된 `minimal: true` 가 무시된다(실제로 이 버그를 냈고 테스트가 잡았다).
그래서 `read()` 가 병합과 별개로 파싱된 원본을 돌려주게 바꿨다.

### 4. 알림

**소리 — WebAudio로 생성 (오디오 파일 없음)**
- `OscillatorNode` + `GainNode`. 단계 전환은 3음(880 / 1108.73 / 1318.51Hz), 전체 종료는 4음 상승 차임.
- gain 램프로 클릭음 제거.
- 자동재생 정책: `AudioContext`는 **첫 사용자 입력 시점에 lazy 생성**하고 `resume()` 호출.
  페이지 로드 시 만들면 `suspended` 상태로 막힌다.
- 패널에 음량 슬라이더 + "소리 테스트" 버튼(교실 스피커 볼륨 사전 확인용).

**종료 30초 전 예고음** — 종료음과 헷갈리지 않도록 **낮은 같은 음(587.33Hz)을 두 번**, 더 작고 짧게 낸다
(종료음은 상승 3음/4음). 단계당 한 번만 울리고(`preWarned`), **45초 이하 단계는 건너뛴다** —
시작과 거의 동시에 울려버리는 것을 막는다.

**초과 시간 카운트업 (`overrunFrom`)** — 종료 후 `00:00` 에 머무는 대신 `+02:30` 으로 올라간다.
- 기준은 **계획된 종료 시각**(`plannedEnd`)이다. 종료를 감지한 시각을 쓰면 틱 지연(최대 200ms)이 섞인다.
- 자동 진행이 켜져 있으면 곧바로 다음 단계가 시작되므로 초과가 없다. **자동 진행을 끈 전환 대기**와
  **전체 종료** 두 경우에만 나타난다.
- 초과 중에도 틱이 살아 있어야 하므로 `beginOverrun()` 이 `startTick()` 을 다시 켠다.
  `tick()` 은 초과 분기를 가장 먼저 처리한다(상태가 `paused`/`finished` 라 기존 가드에 걸리기 때문).
- `armStep()`/`start()` 가 해제하므로, 어떤 조작이든 초과 표시가 끝난다.
- 카운트업은 `fmt(ms, false)` 로 **내림** — 올림을 쓰면 시작 직후 `+0:01` 로 튄다.
- 경고 상태는 `over`(맥박 없는 빨강). 오래 켜져 있을 수 있어 `danger` 의 pulse 는 쓰지 않는다.

**단계별 색상** — `stepList` 항목에 `color`(팔레트 인덱스)를 둔다. 자유 입력 hex 가 아니라 **6색 팔레트**로
제한해 테마별 명암과 경고색 혼동을 원천적으로 막는다(주황·빨강 계열은 팔레트에 없다).
- UI는 행마다 **누르면 순환하는 색 견본 단추** 하나. `select` 보다 좁아 이름 칸을 넓게 남길 수 있고,
  견본에는 항상 그 단계에 실제로 쓰이는 색이 보인다.
- **경고가 색상 지정을 반드시 덮는다.** JS 는 `--step` 을 인라인으로 넣기만 하고 지우지 않으며,
  덮어쓰기는 CSS 특정성에 맡긴다:

  ```css
  body                     { --live: var(--step, var(--base, var(--accent))); }  /* (0,0,1) */
  body[data-warn="warn"]   { --live: var(--warn); }                              /* (0,1,1) → 이김 */
  ```
  `--live` 를 인라인으로 넣으면 인라인이 항상 이겨 경고가 가려진다. 그래서 인라인으로는 `--step`/`--base` 만 넣는다.
- 팔레트 인덱스라 템플릿·내보내기 파일에 자동으로 실려 나가고, `color` 가 없는 예전 데이터는 0으로 읽힌다.

**기본 타이머 색상 (`--base`)** — 팔레트 0번 "기본"이 가리키는 색을 사용자가 직접 고른다.
`<input type="color">` 가 윈도우 색상 선택기를 띄운다. 위 `var()` 사슬의 가운데에 들어가므로
**단계 색상 > 기본 색상 > 테마 강조색** 순이 되고, 경고는 여전히 전부를 덮는다.
- UI 색(`--accent`: 패널 버튼, 포커스 링)은 **건드리지 않는다.** 흐린 색을 고르면 흰 글씨 버튼이 읽히지 않는다.
- **WCAG 명암비를 계산해 3:1 미만이면 경고**한다(큰 글자 기준). 교실 프로젝터는 모니터보다 대비가
  훨씬 나빠서 연한 색을 고르면 뒷자리에서 숫자가 사라진다. 막지 않고 알리기만 한다.
- 값은 `#rrggbb` 만 허용하고(`isHex`), 손상된 저장값은 빈 문자열 = 테마 기본값으로 떨어진다.
- **테마별로 나누지 않고 하나만 저장한다.** 라이트/다크에 같이 적용되지만, 테마를 바꾸면
  명암비 안내가 다시 계산돼 조정할 수 있다. 설정 복잡도를 늘리지 않는 쪽을 택했다.

**현재 시각 / 종료 예정 시각 (`renderClock`)** — 교실에 시계가 없는 경우가 많다.
`지금 10:23 · 전체 종료 10:50 예정` 을 스테이지 좌상단에 작게 표시한다.
- 예정 시각은 **남은 단계를 모두 더해** 계산한다(`projectedEnd`). 종료·초과·빈 큐에서는 `null`.
- 벽시계는 타이머가 멈춰 있어도 흘러야 하므로 **별도 5초 주기**로 갱신한다(틱은 실행 중에만 돈다).
- 숫자만 보기에서는 숨긴다.

**시각**
- 남은 60초: 강조색 → amber / 남은 10초: red + pulse 애니메이션.
- 종료: 화면 전체 3회 플래시.
- 색상은 `--live` 변수로만 교체한다 (아래 "계획에서 바뀐 점" 참고).
- `prefers-reduced-motion: reduce`에서 pulse·플래시는 정적 색상 변화로 대체.

### 5. 프리셋 & 스텝 편집

- 프리셋: 1 / 3 / 5 / 10 / 15 / 25분 + 직접 입력. **클릭 즉시 설정+시작** (교실에서 2클릭은 느리다).
  실행 중에는 프리셋을 비활성 처리해 실수로 초기화되는 것을 막는다.
- 스텝 목록: 행마다 `이름 / 분 / ↑ / ↓ / ✕`, 하단에 "＋ 단계 추가" 와 총 시간 합계.
  이벤트 위임으로 처리하고, 사용자 입력 이름은 `innerHTML`이 아니라 `.value` 대입으로 넣는다.
- 실행 중인 단계 행을 하이라이트.

**직접 입력 — 한 칸에 `분:초`**

칸을 `분`/`초` 두 개로 나누지 않고 한 칸에서 `5:30` 을 받는다. 패널이 가장 좁을 때 280px 까지 줄어드는데,
칸을 하나 더 넣으면 "직접 입력한 시간으로 시작" 버튼이 두 줄로 접힌다 — 반응형 작업에서 막 없앤 실패 방식이다.

```js
durationParts("5")     // { ms: 300000 }   숫자만 쓰면 분
durationParts("5:30")  // { ms: 330000 }
durationParts(":45")   // { ms: 45000 }    분은 비워도 된다
durationParts("5:70")  // { bad:"format" } 초 60 이상은 오타로 본다
durationParts("0")     // { bad:"zero" }
durationParts("601")   // { bad:"range" }
```

- **숫자만 쓰면 분으로 읽는다.** 분만 넣던 기존 습관이 그대로 통해야 하므로 이 해석은 바꾸지 않는다.
  그래서 `type="number"` → `type="text"` 로 바뀌었다(콜론을 받아야 한다). 스피너는 사라진다.
- `inputmode` 를 지정하지 않았다. `numeric` 으로 두면 휴대전화 키패드에 콜론이 없어 기능 자체가 막힌다.
- **틀린 이유를 `format` / `zero` / `range` 로 나눠 돌려준다.** 한 문구에 모든 경우를 담으면
  280px 패널에서 두 줄로 접히고 어느 부분이 문제인지도 흐려진다. 세 문구 모두 한 줄에 들어가는 길이로
  맞췄고(브라우저에서 재서 30자 기준), 테스트가 그 길이를 지킨다.
- **잘못된 입력은 돌아가던 타이머를 건드리지 않는다.** 오타 한 번으로 진행 중인 활동이 날아가면 안 된다.
- 이 안내만 자동으로 사라지지 않는다. 고쳐야 할 값이 아직 칸에 남아 있기 때문이고, 다시 입력하면 지운다.
  템플릿·정각 종료 쪽 안내는 결과 보고라서 몇 초 뒤 사라지는 게 맞다.
- 전각 숫자(`５`)·전각 콜론(`：`)을 반각으로 바꿔 받는다. 한글 입력기에서 흔히 섞여 들어온다.
- 이름은 `durationName()` 하나로 만들어 프리셋과 형식을 맞춘다 — 초가 0이면 `5분 집중`(예전과 동일),
  아니면 `5분 30초 집중`. `presetQueue(min)` 도 `durationQueue(min*60000)` 을 거치게 바꿔 경로를 하나로 합쳤다.

**분 반올림으로 초가 사라지던 곳 (기존 버그)**

길이를 `Math.round(ms / 60000)` 로 찍던 자리가 세 곳 있었다. 1분 미만이면 **`0분`** 이 나왔다.

| 자리 | 예전 | 지금 |
|---|---|---|
| `#note` 의 다음 단계 안내 | `다음: 정리 · 0분` | `다음: 정리 · 45초` |
| 압축 결과 안내(`fitMsg`) | `계획 30분 → 2분 (7%)` | `계획 30분 → 2분 11초 (7%)` |
| 정각 종료 안내(`untilMsg`) | `22:02 까지 약 0분` | `22:02 까지 약 11초` |

- 직접 입력 이전에도 **`fitStepsTo` 로 도달할 수 있는 실제 버그였다.** 압축은 단계가 5초까지 줄어드는 것을 허용하므로
  (`ms < 5000` 이 `tooshort` 하한), 크게 압축하면 1분 미만 단계가 정상적으로 생긴다.
  정각 종료 쪽은 다음 분을 지정하면 남은 시간이 1분 미만이라 곧바로 걸린다.
- 압축 안내는 반올림 때문에 **자기 모순도 있었다.** `2분`은 `30분`의 6.7%인데 괄호에는 `7%` 가 찍혔고,
  실제 값은 `2분 11초` 였다.

**표기를 두 갈래로 나눴다.** 처음엔 세 곳 모두 정확값(`durationLabel`)으로 바꿨는데,
정각 종료 안내가 `약 24분 33초` 가 되면서 오히려 읽기 나빠졌다 — `약` 이라고 해 두고 초까지 붙이는 것은 어긋난다.

| 함수 | 쓰는 곳 | 규칙 |
|---|---|---|
| `durationLabel(ms)` | 다음 단계 안내, 압축 결과, 단계 이름 | 정확값. `45초` / `1분 5초` / `15분` |
| `roughLabel(ms)` | `약 N` 으로 알리는 자리 (정각 종료 안내) | 분으로 뭉갠다. **1분 미만일 때만** 초로 내려간다 |

- 판단 기준은 "그 숫자가 무엇을 말하는가"다. 압축 결과는 **실제로 그렇게 실행된 값**이라 정확해야 하고,
  정각 종료 안내는 **어림 정보**라 분으로 충분하다.
- 분 단위 값의 표기는 두 함수 모두 예전과 같다(`15분`, `약 25분`). 익숙한 화면은 그대로다.
- 12시간 상한 검사도 반올림된 분이 아니라 ms 로 비교하게 바꿨다 (경계에서 반올림에 좌우되지 않도록).

`aria-live` 의 남은 시간 안내는 그대로 뒀다 — `약 N분` 이라 1분 미만도 "약 1분"으로 틀린 말은 아니고,
스크린리더 낭독 주기를 바꾸는 것은 별도 판단이 필요하다.

**활동 단계의 분 입력은 그대로 뒀다.** 초를 넣으려면 저장 형식(`minutes`)과 템플릿 파일 호환까지 걸려서
별건으로 다루는 편이 낫다.

### 5-1. 수업 템플릿 (여러 개 저장)

단원·차시별로 단계 구성이 다르니, 단계 목록을 이름 붙여 여러 개 보관한다.

```js
state.templates      // [{name, steps:[{name, minutes}]}]  이름 순 정렬, 최대 50개
state.activeTemplate // 마지막으로 불러온/저장한 이름
```
- UI는 `활동 단계` 섹션 맨 위에 `select` + `✕`(삭제) / `이름 입력` + `저장` 두 줄.
- **select 에서 고르면 즉시 단계 목록에 반영**된다. 실행 중인 타이머는 건드리지 않는다
  (`loadQueue`가 큐를 별도 스냅샷으로 복사하므로 구조적으로 간섭이 없다).
- 같은 이름으로 저장하면 덮어쓴다. 개수는 늘지 않고, 상한에 걸려도 덮어쓰기는 허용한다.
- **저장·불러오기 모두 깊은 복사**(`cloneSteps`)를 쓴다. 참조를 공유하면 목록을 수정하는 순간
  저장해 둔 템플릿까지 따라 바뀐다.
- 목록이 템플릿과 달라지면 합계 줄에 `· '이름' 에서 수정됨`을 붙인다(`templateDirty()`).
- select 항목에는 `이름 · N단계 M분`을 함께 보여줘 열지 않고도 내용을 짐작할 수 있게 한다.
- 삭제는 `confirm`으로 한 번 확인하고, **현재 단계 목록은 지우지 않는다**.

**저장값 방어** — `sanitizeSteps` / `sanitizeTemplates`로 복원 시 정리한다. 이름 없음·중복 이름·
빈 단계·형식 오류 항목을 걸러내고, 분은 0–600으로 보정, 이름은 문자열로 강제한다.
손상된 JSON이면 빈 목록으로 떨어진다. 같은 정리 함수를 `stepList` 복원과 **파일 가져오기**에도 재사용한다.

### 5-2. 템플릿 파일 내보내기 / 가져오기

템플릿은 그 브라우저의 localStorage 안에만 있어서, 다른 교실 PC로 옮기거나 동료와 나눌 방법이 없었다.
`Blob` + `<a download>` 으로 JSON 파일을 저장하고, `<input type="file">` + `FileReader` 로 읽는다.
서버가 없고 외부 요청도 생기지 않는다.

```json
{ "app": "focus-timer", "version": 1, "exportedAt": "...", "templates": [...] }
```
- **가져오기는 기존 템플릿을 지우지 않는다.** 항상 합치기(merge)이고, 이름이 겹치면
  `실험` → `실험 (2)` 로 번호를 붙인다(`uniqueTemplateName`). 덮어쓰기로 남의 템플릿을 날리는 사고가 없다.
- 최상위가 배열인 파일도 받아준다(직접 손으로 만든 파일 대비).
- 파일 1MB 상한, 템플릿 50개 상한을 넘는 만큼은 건너뛰고 개수를 알려준다.
- 가져온 값도 `sanitizeTemplates` 를 통과시킨다 — 파일은 신뢰할 수 없는 입력이다.

### 6. 부가

- **localStorage**: 설정 / 뽀모도로 / 스텝 목록 / 템플릿 / 활성 템플릿 이름.
  키 접두사 `focusTimer.`, 파싱 실패는 조용히 무시.
  사용자가 아무것도 바꾸지 않은 상태에서는 저장하지 않아, OS 다크모드 자동 적용이 계속 유지된다.
- **화면 꺼짐 방지**: `navigator.wakeLock.request('screen')` — 실행 중 요청, 정지 시 해제.
  미지원 브라우저는 feature detection으로 건너뜀.
- **탭 제목**: `14:23 · 모둠 토의` — 최소화 상태에서도 확인 가능.
- **접근성**: 남은 시간은 `aria-live="polite"`에 **분 단위로만** 갱신(매초 읽으면 스크린리더가 방해된다).
  버튼 `aria-label`, 패널 `aria-expanded` 연동.

### 7. 키보드 단축키

| 키 | 동작 | 키 | 동작 |
|---|---|---|---|
| `Space` | 시작 / 일시정지 | `M` | 표시 방식 순환 |
| `R` | 초기화 | `T` | 테마 전환 |
| `N` | 다음 단계 | `S` | 설정 패널 |
| `F` | 전체화면 | `1`–`6` | 프리셋 |
| `Esc` | 패널 닫기 | | |

입력 필드에 포커스가 있으면 단축키를 무시한다(`Esc`는 포커스 해제).

### 8. 파일 내부 구성

```
<style>   색상 변수(라이트/다크) / 스테이지 / 게이지 / 버튼·폼 / 패널 /
          표시 방식(ring/disk/number) / 알림 애니메이션 / 좁은 화면
<body>    스테이지(단계명·게이지·시간·다음안내·컨트롤) + 설정 패널 + aria-live 영역
<script>  ① 상태 & 기본값  ② 저장/복원  ③ 오디오  ④ 화면 꺼짐 방지
          ⑤ 엔진  ⑥ 큐 생성  ⑦ 렌더  ⑧ 설정 반영  ⑨ 이벤트  ⑩ init
```

---

## 계획에서 바뀐 점

**경고 색상 변수를 분리했다.** 원래는 `--accent`를 amber/red로 교체할 계획이었는데, 그러면 같은 변수를
쓰는 설정 패널의 파란 버튼까지 빨갛게 물든다. 시간·게이지·플래시 전용으로 `--live` 변수를 따로 두고
경고 단계에서는 이것만 바꾼다.

```css
:root { --live: var(--accent); }
body[data-warn="warn"]   { --live: var(--warn); }
body[data-warn="danger"] { --live: var(--danger); }
```

## 검증 결과

### 자동 검증 — 통과 (325건)

DOM을 스텁으로 대체해 node에서 엔진을 실행했다. `AudioContext` 스텁이 울린 음의 주파수를
기록하므로 알림음 종류까지 검증된다. **연속 실행에서 전부 통과** (타이밍 흔들림 없음). 확인된 항목:

**표시 방식 / 원판 기하**
- 부채꼴: 0° 이하 빈 경로, 360° 는 중심선 없는 원, 시작점 12시·90° 끝점 3시·180° 끝점 6시
- `large-arc` 플래그가 180° 경계에서 0/1 로 전환
- 눈금 60개 중 큰 눈금 12개, **숫자 눈금 없음**
- 순환 순서 `ring→disk→number→ring`, 알 수 없는 값 무시, `html[data-display]` 반영
- 링 모드는 `stroke-dashoffset`, 원판 모드는 부채꼴 경로만 갱신 (절반=180°, 가득=완전한 원, 0=빈 경로)
- **설정 이전 4경우**: `minimal:true→number` / `minimal:false→ring` / 이미 `display` 있음 / 손상된 값→ring

**직접 입력 (분:초)**
- 숫자만 → 분 (`5`→5분, `90`→90분), `5:30`→5분 30초, `:45`·`0:45`→45초, `5:3`→5분 3초
- 거부: `5:70`(초 60 이상) · `5:`(콜론만) · `5:3:2` · `5분` · `5.5` · `-5` · `abc` · 빈 값 · 공백만 · `null` · `undefined` · `5:123`
- 경계: `600`·`600:00` 허용 / `601`·`600:01` 거부 / `0`·`0:0`·`:0` 거부
- 공백 섞임(`" 5 : 30 "`) · 전각 숫자(`５:３０`) · 전각 콜론(`5：30`) 모두 정상 해석
- 틀린 이유가 `format` / `zero` / `range` 로 구분되고, 세 경우 모두 안내 문구가 존재하며 한 줄 길이 이내
- 이름: `5분 집중`(초 0, 프리셋과 동일) / `5분 30초 집중` / `45초 집중` / `61분 1초 집중`
- **프리셋 경로를 `durationQueue` 로 합친 뒤에도 이름·길이가 예전과 동일** (5분·25분 확인)
- 45초 큐를 실제로 실려 `remainingMs`·표시(`00:45`)까지 확인

**길이 표기 — 0분 버그**
- `durationLabel`: `45초` · `5초`(압축 하한) · `29초` · `31초` — 예전에는 반올림으로 `0분`/`1분` 이 되던 구간
- `durationLabel`: `1분 30초`(예전에는 `2분`) / `1분` / `5분` / `15분` / `60분` — 분 단위는 예전과 동일
- `roughLabel`: `25분`(1500000ms) · **`25분`(24분 33초 → 어림수로 뭉갬)** · `1분` · `33초` · `59초` · `1초`
- 실제 큐를 실어 `#note` 문구 확인: `다음: 정리 · 45초` / `다음: 정리 · 1분 30초` / `다음: 활동 · 15분`
- **버그 발생 경로 재현** — `fitStepsTo` 로 10분 계획을 100초로 압축해 두 단계 모두 1분 미만으로 만든 뒤,
  `#note` 에 `0분` 이 없고 초 표기가 나오는 것을 확인

**설정 패널 접이식 구획**
- 기본값은 `빠른 시작` 하나만 펼침, 나머지는 제목만
- 펼친 구획만 복원, 여닫은 상태가 저장·복원됨
- 모르는 id 는 버림 / 배열이 아닌 손상된 값 → 기본값 / **openSecs 가 없는 예전 저장값도 기본값으로 동작**

**기본 타이머 색상**
- `isHex`: 3자리·`#` 없음·색이름·빈값·`null`·hex 아닌 문자 모두 거부
- 명암비: 흰↔검정 21:1, 동일색 1:1, 인자 순서 대칭, 잘못된 입력 0
- 기본 파랑(라이트·다크 모두) 3:1 이상 / 노란색은 3:1 미만 → 경고 발생 확인
- `--base` 적용·해제, 선택기 값 동기화, 잘못된 값은 해제로 처리
- 단계 색상이 있으면 `--step` 이 우선하고 `--base` 는 남는다 (사슬은 CSS 담당)
- **경고 구간에서도 `--base`/`--step` 을 지우지 않는다**
- 테마 전환 시 선택기 기본색이 따라 바뀜, 저장·복원, 손상된 저장값 방어

**단계 색상 / 시각 표시**
- 팔레트 범위 밖·음수·누락 색 번호를 0으로 보정, 문자열 숫자 허용, 색 없는 예전 데이터 하위 호환
- 색이 실행 큐 → 템플릿 → 내보내기 파일까지 보존됨
- 지정 색은 `--step` 으로, 기본 색이면 `--step` 제거
- **경고 구간에서도 JS 는 `--step` 을 지우지 않는다** (덮어쓰기는 CSS 특정성 담당 — 위 설계 참고)
- `projectedEnd` 가 남은 단계를 모두 더함(3.5분 조합 오차 100ms 미만), 종료·초과·빈 큐에서 `null`
- 단일 타이머는 `종료`, 여러 단계는 `전체 종료` 로 표기 / 설정 off 시 표시 비움
- `hhmm` 한 자리 시·분 0 채움, 24시간 표기

**예고음 / 초과 시간**
- 세 알림음이 주파수로 구분됨: 예고 `587.33 ×2` / 단계 종료 3음 / 전체 종료 4음
- 30초 경계 통과 시 1회만 울림, 45초 이하 단계는 건너뜀, 설정 off 시 무음
- 초과 계측 기준이 **계획된 종료 시각** (감지 지연 40ms 미만)
- `+00:01` → `+00:02` 로 실제 증가, 게이지 비움, 경고 상태 `over`, 탭 제목 반영
- 자동 진행 ON → 초과 없음 / OFF → 전환 대기 중 계측
- 조작 시 해제, 설정 토글을 끄면 즉시 걷힘
- `fmt(ms,false)` 내림 / `fmt(ms,true)` 올림

**단계 압축**
- 합계가 남은 시간과 **반올림 오차 0**으로 일치 — 나눠떨어지지 않는 조합 7가지에서도 검증
- 각 단계의 비율이 원래 계획과 동일, 단계 수·이름 유지
- **`stepList` 원본이 그대로 남는다** (교사의 계획이 파괴되지 않음)
- 시간이 남을 때 늘리는 방향도 동작
- 지난 시각 / 빈 목록 / 5초 미만 단계 발생 시 거부

**초기화 보호**
- 실행 중 `confirm` 표시, 문구에 남은 시간 포함
- 취소하면 타이머 유지, 확인하면 초기화
- 3초 미만·정지 상태에서는 묻지 않음

**템플릿 파일 내보내기 / 가져오기**
- `app`/`version`/`exportedAt` 헤더와 템플릿 내용이 담긴다
- **가져오기가 기존 템플릿을 지우지 않는다** — 이름 충돌은 `기존 (2)` 로, 원본은 그대로
- 최상위 배열 형식 허용, 깨진 JSON·빈 템플릿 거부, 50개 상한 초과분 건너뛰기
- 내보낸 파일을 그대로 다시 가져오는 왕복 검증

**정각 종료 모드**
- 마감시각이 목표 시각과 **오차 0**으로 일치
- **일시정지 후 재개해도 목표 시각이 밀리지 않는다** (정지 동안 흐른 시간이 반영됨)
- `reset()`도 벽시계 기준 재계산, 게이지 기준(`totalMs`)은 원래 구간 유지
- 지난 시각 거부, 24시/60분 초과·형식 오류 → `null`
- 목표 시각 경과 후 실행 시 안내 표시 (조용히 실패하지 않음)
- `loadQueue()`가 정각 상태를 해제 → 프리셋은 다시 상대 시간으로 동작
- 목표 시각 도달 시 실제 종료

**엔진**
- **단계 간 드리프트 없음** — 600ms × 3단계 실행에서 각 마감시각이 `t0+600 / +1200 / +1800ms`에 오차 25ms 이내
- 일시정지 중 시간 정지, 재개 시 정지 시점부터 이어짐
- 뽀모도로 큐: 4라운드 → 8단계, 4번째 휴식만 긴 휴식
- 스텝 큐: 0분 단계 제외, 이름 공백 시 "활동" 대체
- `next()` / `reset()` / 마지막 단계 종료 처리
- 게이지 `stroke-dashoffset`: 가득 → 0, 절반 → 원주/2
- `fmt()` 표기: 올림 처리로 5분 시작 시 `04:59`가 아닌 `05:00`

**템플릿**
- 저장 / 덮어쓰기 / 불러오기 / 삭제, 이름 순 정렬
- **깊은 복사** — 저장 후 목록을 고쳐도 템플릿이 안 바뀌고, 불러온 뒤 고쳐도 원본이 안 바뀐다
- `templateDirty()`: 수정 감지, 덮어쓴 직후 해제
- 이름 없음 / 1분 이상 단계 없음 / 50개 초과 거부, 상한에서도 덮어쓰기는 허용
- 활성 템플릿 삭제 시 선택 해제, 현재 단계 목록은 보존
- select 항목 표기 `이름 · N단계 M분`, 삭제 버튼 활성/비활성

**저장값 방어**
- localStorage 왕복 저장·복원 (설정 / 뽀모도로 / 단계 / 템플릿 / 활성 이름)
- 중복 이름·무명·빈 단계·형식 오류 템플릿 제거
- 분 값 0–600 보정, 이름 문자열 강제
- 손상된 JSON → 앱이 죽지 않고 기본값/빈 목록으로 진행

**기타**
- **외부 요청 0** — `http`, `src=`, `@import`, `url(`, `fetch` 전부 미검출

### 레이아웃 검증 — 실제 브라우저 측정 (246건 통과)

DOM 스텁은 레이아웃을 계산하지 않으므로 node 로는 확인할 수 없다.
대신 로컬 http 서버로 파일을 띄우고, **iframe 하나를 만들어 크기만 바꿔가며 실제 좌표를 읽었다.**
매번 새로 불러오지 않고 `setDisplay()` · `togglePanel()` 을 직접 불러 상태를 바꾸는 방식이라 빠르다.

**41개 해상도 × 패널 열림·닫힘 × 표시 방식 3종 = 246건.** 각 조합에서 확인한 것:

- `#startBtn` · `#fsBtn` 의 아래·오른쪽 끝이 뷰포트를 넘지 않는지 (잘림)
- `.stage-head` · `.clock-wrap` 의 위쪽이 음수가 아닌지 (위로 밀려남)
- 패널이 열렸을 때 게이지·버튼이 패널 왼쪽 경계를 넘지 않는지 (가려짐).
  전체 화면 시트일 때는 가리는 것이 의도이므로 제외했다.
- `.controls` 가 몇 줄인지

고치기 전 같은 방식으로 잰 수치 — 이 작업의 근거다.

| | 고치기 전 | 지금 |
|---|---|---|
| 버튼·게이지가 화면 밖으로 잘림 | 16건 (740×360, 640×360, 568×320, 1280×300) | **0건** |
| 패널이 타이머를 덮음 | 69건 (860×540 이하 전부) | **0건** |

조작 버튼이 두 줄로 접히는 경우는 폭 375px 미만에서만 남는다. 그 크기에서는 세로가 넉넉해 잘리지 않는다.

### 브라우저에서 직접 볼 항목 (스텁으로 확인 불가)

- 프로젝터 판독성 (`F` 전체화면 후 교실 뒤쪽 거리에서)
- 알림음 실제 재생·음량 — "소리 테스트" 버튼으로 수업 전 확인
- 60초/10초 경고 색상 전환, 종료 플래시
- Wake Lock·전체화면 (브라우저별 지원 차이)

빠른 확인: `1` 키로 1분 타이머, 또는 뽀모도로 집중/휴식을 1분으로 바꿔 실행.

### 알려진 제약

- **소리는 첫 클릭·키 입력 이후에만** 난다. 브라우저 자동재생 정책이라 우회할 수 없다.
- `file://`로 열 때 Chrome·Edge·Firefox는 설정이 저장되지만 **Safari는 막는다**.
  저장 실패는 `try/catch`로 흘려보내 타이머 자체는 정상 동작하고, 새로고침 시 기본값으로 돌아간다.

## 이번 범위에서 제외

추천 목록의 항목은 모두 반영됐다. 남은 것은 처음부터 권하지 않은 것들이다.

- **모둠별 다중 타이머** — 화면 분할과 상태 관리가 커지는데, 모둠 순회는 단계 목록으로 충분하다
- **사용 기록·통계** — 교사가 실제로 다시 들여다볼 일이 드물다
- **배경 화이트노이즈** — 교실에서는 오히려 방해가 될 가능성이 높다
- **링크 공유 / 여러 화면 실시간 동기화** — 서버가 필요해 단일 파일 구조를 벗어난다
  (템플릿 파일 주고받기로 대체됨)
