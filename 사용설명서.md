# 수업용 집중 타이머 — 사용설명서

교실 프로젝터·TV에 띄워 쓰는 타이머입니다. 설치·인터넷·로그인이 필요하지 않습니다.

---

## 1. 시작하기

`focus-timer.html` 파일을 **더블클릭**하면 브라우저에서 열립니다. 끝입니다.

- USB에 복사해 다른 교실 PC에서 열어도 그대로 동작합니다.
- 인터넷이 끊겨 있어도 됩니다. 외부에 아무것도 보내지 않습니다.
- 처음 열면 `05:00`과 함께 기본 수업 흐름(도입 5분 → 모둠 활동 15분 → 발표·정리 10분)이 준비돼 있습니다.

> **바로 시작하려면**: `1` 키를 누르면 1분 타이머가, `F` 키를 누르면 전체화면이 됩니다.

---

## 2. 화면 구성

```
 지금 10:23 · 전체 종료 10:50 예정        ← 벽시계 시각과 예상 종료 시각

        모둠 활동              3 / 5      ← 지금 하는 활동 / 몇 번째 단계인지
           ╭───────────╮
           │   14:23   │                 ← 남은 시간 (게이지가 함께 줄어듭니다)
           ╰───────────╯                   표시 방식은 3가지 — 8장 참고
        다음: 발표 · 10분                 ← 이어질 활동 미리보기

   [시작] [초기화] [다음 ▶] [⛶ 전체화면]
```

왼쪽 위 시각 표시는 **교실에 시계가 없을 때** 쓰라고 넣었습니다. `전체 종료 10:50 예정` 은 남은 단계를 모두 더해 계산한 값이라, 수업이 시간 안에 끝날지 바로 보입니다. 필요 없으면 설정에서 끌 수 있습니다.

> 예정 시각은 **쉬지 않고 진행했을 때** 기준입니다. 일시정지하거나 단계 사이에서 기다리면 그만큼 늦어집니다.

오른쪽 위 **⚙ 설정** 버튼(또는 `S` 키)으로 설정 패널을 엽니다.

### 설정 패널은 제목만 보입니다

패널을 열면 다섯 개의 제목이 한 줄씩만 보입니다. **제목을 클릭하면 그 안의 설정이 펼쳐집니다.**

```
빠른 시작    ▾
활동 단계    ▾
뽀모도로     ▾
표시 & 소리  ▾
단축키       ▾
```

- 여러 개를 동시에 펼쳐 둘 수 있습니다.
- 마지막으로 펼쳐 둔 상태를 기억하므로, 자주 쓰는 것만 열어 두면 다음에도 그대로 열려 있습니다.
- 처음에는 **빠른 시작**만 펼쳐져 있습니다.

### 화면 크기에 맞춰 알아서 바뀝니다

프로젝터, 교사 노트북, 태블릿, 휴대전화 어디서 열어도 잘리는 곳 없이 들어갑니다. 따로 설정할 것은 없습니다.

| 화면 | 이렇게 보입니다 |
|---|---|
| 프로젝터 · TV · 노트북 | 타이머가 왼쪽, 설정 패널이 오른쪽에 나란히. 패널을 열면 게이지가 그만큼 줄어듭니다 |
| 태블릿 | 같은 배치에 패널만 좁아집니다 |
| 좁은 화면 (가로 680px 미만) | 설정 패널이 **화면 전체를 덮는 형태**로 바뀝니다. 닫으면 타이머가 화면을 온전히 씁니다 |
| 창을 낮게 줄였을 때 | 게이지가 세로에 맞춰 작아져서 아래 버튼이 잘리지 않습니다 |

> 좁은 화면에서 설정 패널이 타이머를 다 덮는 것은 의도한 동작입니다. 패널과 타이머를 나란히 두면 둘 다 너무 작아져서, **볼 때는 하나씩** 보이도록 했습니다. 오른쪽 위 **⚙ 설정** 버튼(또는 `S`·`Esc` 키)으로 닫으면 타이머로 돌아옵니다.

| 버튼 | 하는 일 |
|---|---|
| **시작 / 일시정지** | 같은 버튼이 상황에 따라 바뀝니다 |
| **초기화** | 첫 단계로 되돌립니다. 실행 중이면 한 번 확인을 묻습니다 |
| **다음 ▶** | 남은 시간을 버리고 다음 단계로 넘어갑니다 |
| **⛶ 전체화면** | 브라우저 메뉴를 감추고 화면을 꽉 채웁니다 |

실행 중에 마우스를 3초간 움직이지 않으면 버튼이 흐려집니다. 화면을 가리지 않게 하려는 것이고, 마우스를 움직이면 다시 나타납니다.

---

## 3. 가장 빠른 사용법 — 프리셋

설정 패널 → **빠른 시작**

```
[ 1분 ] [ 3분 ] [ 5분 ]
[10분 ] [15분 ] [25분 ]
```

**누르면 곧바로 시작합니다.** 키보드 `1`~`6`도 같은 순서로 대응합니다 (`1`=1분 … `6`=25분).

### 원하는 시간이 없을 때 — 직접 입력

아래 칸에 시간을 넣고 **직접 입력한 시간으로 시작**을 누릅니다. `Enter` 키로도 됩니다.

| 이렇게 넣으면 | 이렇게 시작합니다 |
|---|---|
| `7` | 7분 — **숫자만 쓰면 분입니다** |
| `5:30` | 5분 30초 |
| `:45` 또는 `0:45` | 45초 |
| `1:05` | 1분 5초 |

짧은 활동에 쓰면 좋습니다 — 자리 정리 45초, 짝과 생각 나누기 90초, 받아쓰기 한 문항 30초처럼요.

- **숫자만 쓰면 분**입니다. 예전처럼 분만 넣던 방식이 그대로 통하니 새로 익힐 것은 없습니다.
- 최대 **600분**(10시간)까지 넣을 수 있습니다.
- 잘못 넣으면 칸 아래에 무엇이 틀렸는지 한 줄로 뜨고, **돌아가던 타이머는 건드리지 않습니다.** 다시 입력하기 시작하면 안내가 사라집니다.

> `5:70` 처럼 초가 60을 넘으면 오타로 보고 거부합니다. 70초를 원하면 `1:10` 으로 넣으세요.

> 실행 중에는 프리셋 버튼이 비활성화됩니다. 돌아가는 활동이 실수로 날아가는 걸 막기 위한 것으로, **일시정지하면 다시 눌릴 수 있게 됩니다.**

---

## 4. 수업 시간에 맞추기

수업은 길이보다 **끝나는 시각**이 정해져 있습니다. 두 가지 방법이 있습니다.

### 4-1. 그 시각까지 하나로 재기

설정 패널 → **빠른 시작** 맨 아래

```
[ 10:50 ] [ 이 시각까지 ]
```

10시 50분까지 남은 시간을 카운트다운합니다. 화면에는 `10:50 까지`로 표시됩니다.

**이 모드는 일시정지해도 종료 시각이 밀리지 않습니다.** 벽시계는 멈추지 않으니까요. 5분 멈췄다 재개하면 남은 시간이 5분 줄어 있고, 10:50이라는 종료 시각은 그대로입니다. 갑작스러운 안내방송이나 대피 훈련으로 수업이 끊겨도 실제 종료 시각은 변하지 않는다는 뜻입니다.

### 4-2. 계획한 단계를 남은 시간에 맞춰 압축하기

수업이 늦게 시작됐을 때 씁니다. 설정 패널 → **활동 단계** 맨 아래

```
[ 10:50 ] [ 종료 시각에 맞춰 압축 시작 ]
```

45분짜리 계획을 남은 36분에 **비율대로 줄여서** 실행합니다.

| | 원래 계획 | 36분에 맞춘 실행 |
|---|---|---|
| 도입 | 5분 | 4분 |
| 모둠 활동 | 30분 | 24분 |
| 정리 | 10분 | 8분 |

- **저장해 둔 계획은 그대로 남습니다.** 이번 실행만 줄어들고, 단계 목록과 템플릿은 45분 그대로입니다. 다음 반에서 다시 온전하게 쓸 수 있습니다.
- 시간이 남을 때는 늘리는 방향으로도 동작합니다.
- 이 방식은 **일시정지하면 종료 시각이 그만큼 밀립니다** (4-1과 다른 점). 중간에 다시 맞추고 싶으면 버튼을 한 번 더 누르면 됩니다.

> **시각 입력 칸이 두 개인 이유**: 위쪽(빠른 시작)은 단일 타이머용, 아래쪽(활동 단계)은 단계 압축용입니다. 서로 독립적입니다.

---

## 5. 활동 단계로 수업 흐름 만들기

설정 패널 → **활동 단계**

```
[ 도입          ] [  5 ] [■] [↑][↓][✕]
[ 모둠 활동      ] [ 15 ] [■] [↑][↓][✕]
[ 발표·정리      ] [ 10 ] [■] [↑][↓][✕]
                ＋ 단계 추가
          총 3단계 · 30분
```

- 이름과 분을 직접 고쳐 씁니다. **입력하는 즉시 저장**되므로 따로 저장 버튼을 누를 필요가 없습니다.
- `↑` `↓` 로 순서를 바꾸고, `✕` 로 지웁니다.
- **`■` 색상 단추**를 누르면 색이 바뀝니다 (기본 → 파랑 → 초록 → 청록 → 보라 → 분홍 → 기본…). 그 단계가 실행되는 동안 큰 숫자와 게이지가 그 색이 됩니다. 개인 활동은 파랑, 모둠은 초록처럼 정해두면 학생들이 지금 무슨 활동인지 색만 보고 압니다.
  `기본` 은 8장에서 고른 **기본 타이머 색상**을 씁니다.

> 색을 지정해도 **1분 전 주황색·10초 전 빨간색 경고는 그대로 우선합니다.** 색상 때문에 시간 경고를 놓치는 일은 없습니다.
>
> 색은 템플릿과 내보내기 파일에도 함께 저장됩니다. 실행 중에 색을 바꾸면 다음 실행부터 적용됩니다.
- **0분으로 둔 단계는 실행에서 건너뜁니다.** 지우지 않고 잠시 빼두고 싶을 때 편합니다.
- 다 짜고 **단계 순서대로 시작**을 누르면 첫 단계부터 진행합니다.

### 단계 끝나면 자동으로 다음 시작

| 켬 (기본) | 알림음이 울리고 곧바로 다음 단계가 시작됩니다 |
|---|---|
| **끔** | 알림음 후 멈춰서 기다립니다. 화면에 `다음 단계 준비됨 — 시작을 누르세요` 가 뜹니다 |

발표처럼 학생이 준비될 때까지 기다려야 하는 활동이 있으면 **끄는 쪽**이 편합니다.

---

## 6. 수업 템플릿 — 여러 개 저장하고 나누기

단원·차시마다 흐름이 다르니, 단계 구성을 이름 붙여 보관할 수 있습니다.

```
[ 저장된 템플릿 3개 — 선택하면 불러옵니다  ▾ ] [✕]
[ 3단원 실험                              ] [저장]
[ ↓ 내보내기 ]  [ ↑ 가져오기 ]
```

### 저장하고 불러오기

1. 단계 목록을 원하는 대로 짭니다
2. 이름 칸에 `3단원 실험` 같은 이름을 넣고 **저장**
3. 다음 시간에는 위쪽 목록에서 고르면 바로 불러옵니다

- 목록 항목에는 `3단원 실험 · 4단계 45분` 처럼 **단계 수와 총 시간이 함께** 보입니다.
- 같은 이름으로 저장하면 덮어씁니다.
- 불러온 뒤 단계를 고치면 합계 줄에 `· '3단원 실험' 에서 수정됨` 이 붙습니다. 저장을 잊지 않도록 알려주는 표시입니다.
- **`✕` 로 템플릿을 지워도 화면의 단계 목록은 남습니다.**
- 최대 50개까지 저장됩니다.

> 실행 중에 템플릿을 불러와도 **돌아가는 타이머는 건드리지 않습니다.** 다음 실행에 쓸 목록만 바뀝니다.

### 다른 PC·다른 선생님과 나누기

템플릿은 그 브라우저 안에만 저장됩니다. 옮기려면 파일로 주고받습니다.

- **↓ 내보내기** → `수업타이머-템플릿-20260730.json` 파일이 저장됩니다
- **↑ 가져오기** → 그 파일을 고르면 템플릿이 추가됩니다

**가져오기는 기존 템플릿을 지우지 않습니다.** 항상 합쳐지고, 이름이 겹치면 `3단원 실험 (2)` 처럼 번호를 붙입니다. 동료와 파일을 주고받다가 자기 템플릿이 사라지는 일은 없습니다.

> 새 학기에 다른 PC를 쓰게 되거나 브라우저 데이터를 정리해야 할 때를 대비해, 템플릿을 잘 짜뒀다면 한 번 내보내서 보관해 두는 걸 권합니다.

---

## 7. 뽀모도로

설정 패널 → **뽀모도로**. 집중과 휴식을 자동으로 번갈아 반복합니다.

| 항목 | 뜻 | 기본값 |
|---|---|---|
| 집중 | 한 번에 집중하는 시간 | 25분 |
| 휴식 | 짧은 휴식 | 5분 |
| 긴 휴식 | 여러 번 반복한 뒤의 휴식 | 15분 |
| 반복 횟수 | 집중을 몇 번 할지 | 4회 |
| 긴 휴식 주기 | 몇 번째마다 긴 휴식을 넣을지 | 4 |

기본값이면 `집중25 → 휴식5` 를 세 번 반복하고, 네 번째 집중 뒤에 `긴 휴식15` 가 붙습니다. 화면에는 `집중 2/4` 처럼 진행이 표시됩니다.

> 자습이나 긴 실습 시간에 맞고, 40~50분 단위 수업에는 **활동 단계**(5장)가 더 잘 맞습니다.

---

## 8. 화면과 소리 조절

설정 패널 → **표시 & 소리**

### 표시 방식 (`M` 키로 순환)

세 가지 중에 고릅니다. `M` 키를 누르면 순서대로 바뀝니다.

| 방식 | 모습 | 언제 쓰나 |
|---|---|---|
| **링 + 숫자** | 얇은 테두리가 줄어들고 숫자가 큼 | 기본값. 숫자를 크게 보여주고 싶을 때 |
| **원판 + 숫자** | 색 면적이 12시 방향으로 줄어들고 가운데에 숫자 | **저학년.** 남은 양이 면적으로 보여 시계를 못 읽어도 이해합니다 |
| **숫자만** | 장식 없이 숫자만 최대 크기로 | 교실이 넓어 판독이 어려울 때 |

**원판 + 숫자** 는 교실용 타임타이머와 같은 방식입니다. 원판 한 바퀴가 **그 단계의 전체 시간**이라, 5분 단계든 45분 단계든 가득 찬 상태로 시작해서 항상 잘 보입니다. 눈금은 60칸(5칸마다 굵게)이고 숫자는 붙이지 않았습니다 — 한 바퀴의 길이가 단계마다 다르니 "10분" 같은 눈금 숫자가 거짓이 되기 때문입니다.

> 원판 모드에서는 숫자가 가운데 구멍 안에 들어가야 해서 **링 모드보다 작습니다.** 숫자를 크게 보여주는 게 더 중요하면 링 모드나 숫자만 모드를 쓰세요.

### 기본 타이머 색상

색 사각형을 클릭하면 **윈도우 색상 선택기**가 열립니다. 단계 색상을 `기본` 으로 둔 단계와, 단계를 쓰지 않는 프리셋·뽀모도로가 이 색을 씁니다.

- 아래에 **명암비**가 표시됩니다. `3:1 미만` 이면 경고가 뜹니다 — 교실 프로젝터는 모니터보다 대비가 훨씬 나빠서 연한 색을 고르면 뒷자리에서 숫자가 사라집니다. 막지는 않으니 판단해서 쓰세요.
- `테마 기본값으로` 버튼으로 언제든 되돌립니다.
- 색은 **하나만 저장되어 밝은 테마·어두운 테마에 함께 적용**됩니다. 어두운 테마에서 고른 색이 밝은 테마에서 흐릴 수 있는데, 테마를 바꾸면 명암비 안내가 다시 계산되니 그때 조정하세요.
- 단계 목록의 `기본` 색 견본도 고른 색으로 바뀌어서, 목록에서 바로 확인됩니다.

**1분 전 주황·10초 전 빨강 경고는 이 색을 덮습니다.** 어떤 색을 골라도 시간 경고를 놓치지 않습니다.

### 현재 시각 표시

`현재 시각 · 종료 예정 시각 표시` 를 끄면 왼쪽 위 표시가 사라집니다. 교실에 시계가 있거나 화면을 더 비우고 싶을 때 끄세요.

### 알림음

세 가지 소리가 서로 구분되게 되어 있습니다.

| 시점 | 소리 |
|---|---|
| 종료 30초 전 | 낮은 음 두 번 (작고 짧게) — **마무리 신호** |
| 단계 종료 | 3음 상승 |
| 전체 종료 | 4음 상승 |

- **수업 전에 「소리 테스트」 버튼으로 교실 스피커 볼륨을 미리 맞춰두세요.**
- 소리를 아예 끄고 싶으면 `종료 알림음` 체크를 해제합니다. 예고음만 끄려면 `종료 30초 전 예고음` 을 해제합니다.
- 45초 이하로 짧은 단계에서는 예고음이 울리지 않습니다 (시작과 거의 동시에 울려버리는 걸 막기 위한 것입니다).

### 초과 시간 표시

단계가 끝난 뒤에도 넘어가지 않고 있으면, 화면이 `00:00` 에서 멈추는 대신 **얼마나 넘겼는지 빨간색으로 올라갑니다.**

```
        +02:30        ← 계획된 종료 시각보다 2분 30초 초과
```

- 수업이 밀렸을 때 다음 활동을 줄일 판단이 섭니다.
- **자동 진행을 켜두면 나오지 않습니다** — 곧바로 다음 단계가 시작되니 초과가 없습니다. 자동 진행을 끈 경우(전환 대기 중)와 전체 단계가 끝난 뒤에 나타납니다.
- 시작·초기화 등 아무 조작이나 하면 사라집니다.
- 필요 없으면 `종료 후 초과 시간 표시` 를 해제하세요.

> 초과 시간은 **계획된 종료 시각**을 기준으로 셉니다. 화면이 종료를 감지하는 데 걸린 시간이 섞이지 않으므로, 표시된 값이 실제 초과와 일치합니다.

### 테마 (`T` 키)

밝은 화면 / 어두운 화면을 전환합니다. 처음 열 때는 PC 설정을 따라갑니다. 교실 조명과 프로젝터 밝기에 따라 잘 보이는 쪽을 고르세요.

### 시간이 임박하면

| 남은 시간 | 화면 |
|---|---|
| 평소 | 그 단계에 지정한 **색상** (지정 안 하면 기본 타이머 색상) |
| 30초 전 | (소리) 예고음 두 번 |
| 1분 이하 | 숫자와 게이지가 **주황색** — 단계 색상을 덮습니다 |
| 10초 이하 | **빨간색** + 숫자가 커졌다 작아짐 |
| 종료 | 화면 전체가 세 번 번쩍이고 알림음 |
| 종료 후 | `+02:30` 초과 시간이 빨간색으로 올라감 |

PC에서 「동작 줄이기(reduce motion)」를 켜두었다면 깜빡임 없이 색상만 바뀝니다.

---

## 9. 단축키

입력 칸에 커서가 있을 때는 단축키가 동작하지 않습니다 (`Esc` 로 커서를 뺄 수 있습니다).

| 키 | 동작 | 키 | 동작 |
|---|---|---|---|
| `Space` | 시작 / 일시정지 | `M` | 표시 방식 순환 |
| `R` | 초기화 (실행 중이면 확인) | `T` | 테마 전환 |
| `N` | 다음 단계 | `S` | 설정 패널 |
| `F` | 전체화면 | `1`~`6` | 프리셋 |
| `Esc` | 설정 패널 닫기 | | |

---

## 10. 자주 겪는 상황

**소리가 안 납니다**
브라우저 정책상 **화면을 한 번이라도 클릭하거나 키를 누른 뒤에야** 소리가 납니다. 수업 시작 전에 프리셋 버튼이나 「소리 테스트」를 한 번 눌러두면 이후에는 정상입니다.

**프리셋 버튼이 눌리지 않습니다**
타이머가 실행 중이면 잠깁니다. `Space` 로 일시정지하면 다시 눌립니다.

**다른 탭을 보다 돌아왔더니 시간이 맞나요?**
정확합니다. 남은 시간을 1초씩 빼는 방식이 아니라 **끝나는 시각을 기준으로 계산**하기 때문에, 브라우저가 백그라운드 탭을 느리게 돌려도 시간이 밀리지 않습니다.

**수업 중에 화면이 절전으로 꺼집니다**
타이머가 실행 중이면 화면 꺼짐을 막습니다. 다만 브라우저에 따라 지원하지 않는 경우가 있어서, 오래 쓸 예정이면 PC 전원 설정에서 절전 시간을 늘려두는 게 확실합니다.

**「이미 지난 시각입니다」가 뜹니다**
오후에 `09:00` 처럼 이미 지나간 시각을 넣은 경우입니다. 자정을 넘겨 24시간 타이머가 되는 사고를 막기 위해 막아둔 것으로, 앞으로의 시각을 넣어주세요. (12시간 이내까지 가능)

**「남은 시간이 너무 짧아 단계를 나눌 수 없습니다」**
단계 압축(4-2)에서 남은 시간이 단계 수에 비해 지나치게 짧을 때 나옵니다. 단계를 줄이거나 단일 타이머(4-1)를 쓰세요.

**설정이 새로고침하면 사라집니다**
Safari에서 파일을 직접 열면 저장이 막힙니다. Chrome·Edge·Whale·Firefox를 쓰시면 됩니다. 타이머 자체는 어느 브라우저에서든 정상 동작합니다.

**작업표시줄에서 남은 시간을 보고 싶습니다**
브라우저 탭 제목에 `14:23 · 모둠 활동` 으로 표시됩니다. 창을 최소화해도 확인할 수 있습니다.

---

## 11. 알아두면 좋은 제약

- 템플릿 **50개**, 가져올 파일 **1MB** 까지
- 단계 하나는 **최대 600분**
- 정각 종료는 **12시간 이내**
- 저장 위치는 그 PC의 그 브라우저입니다. 브라우저 데이터를 지우면 함께 사라지니, 중요한 템플릿은 **내보내기로 파일 보관**을 권합니다
- 여러 화면·여러 PC 간 실시간 동기화는 지원하지 않습니다 (서버가 필요한 기능입니다)

---

## 12. 수업 전 30초 점검

1. 파일 열고 `F` 로 전체화면
2. 「소리 테스트」로 스피커 볼륨 확인
3. 교실 뒷자리 거리에서 읽히는지 확인 — 안 보이면 `M` 으로 표시 방식 바꾸기
4. 조명에 따라 `T` 로 테마 선택
5. 오늘 쓸 템플릿 불러오기
